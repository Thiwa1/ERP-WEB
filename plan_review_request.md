1. **Implement `Customer_ProfitLossReportGenerator` in `profit_loss_report.py`:** Add a subclass of `ProfitLossReportGenerator` to handle custom queries grouping or filtering by customer (using `entry_sub_account_code` mapped to `sub_accont_for_new_account` using customer names)
2. **Implement Backend Route `/customer_profit_loss` in `app.py`:** Create `def customer_profit_loss()` similar to `profit_loss()`, using the new `Customer_ProfitLossReportGenerator`.
3. **Implement Backend Route `/customer_balance_sheet` in `app.py`:** Create `def customer_balance_sheet()`, mirroring `balance_sheet()`, but appending a filter for `entry_sub_account_code`. The page will take `customer_id` via a drop-down or as argument, and fetch the `sub_account_code` corresponding to the customer for use in the SQL filter in `entry_details`.
4. **Create HTML Templates for both:** Use bash `run_in_bash_session` to duplicate and modify `templates/profit_loss.html` to `templates/customer_profit_loss.html`, and `templates/balance_sheet.html` to `templates/customer_balance_sheet.html`. Add drop-downs to select customers from `suppliers WHERE Is_Customer=1`.
5. **Verify File Creation:** Run `ls templates/customer*` and `cat` modified templates to verify correctness.
6. **Update Navigation Menus:** Using `replace_with_git_merge_diff`, update `templates/base.html` and `templates/index.html` to include links for the Customer Balance Sheet and Customer Profit & Loss, as illustrated in the provided image.
7. **Verify Tests:** Run `PYTHONPATH=. python tests/run_tests.py` to ensure changes didn't break things.
8. **Pre-commit Checks:** Call `pre_commit_instructions` tool and complete required pre-commit steps to ensure proper testing, verifications, reviews, and reflections are done.
